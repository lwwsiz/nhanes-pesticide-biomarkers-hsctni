# Tested environment

The core-analysis workflow was tested on Windows with Python 3.11.15, NumPy 2.4.6, and pandas 2.3.3. The machine-readable environment record is provided in `results/reproduced/environment_versions.json`.

Install compatible dependencies with:

```powershell
python -m pip install -r requirements.txt
```
